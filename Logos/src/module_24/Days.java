package module_24;

public enum Days {
  MONDAY(1), THUESDAY(2), WEDNESDAY(3), THURSDAY(4), FRIDAY(5), SATURDAY(6), SUNDAY(7);

  private int number;

  Days(int number) {
    this.number = number;
  }
}
